<%= form_for [:admin, request] do |f| %>
  <%= f.select :status, Request.statuses.keys.map {|status| [t("request_status.#{status}"), status]} %>
  <%= f.submit t("save"), class: "btn btn-success" %>
<% end %>
